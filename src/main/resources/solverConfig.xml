<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://www.optaplanner.org/xsd/solver" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://www.optaplanner.org/xsd/solver https://www.optaplanner.org/xsd/solver/solver.xsd">
    <!-- Define the model -->
    <solutionClass>com.example.parkingsolver.domain.ParkingSolution</solutionClass>
    <entityClass>com.example.parkingsolver.domain.Vehicle</entityClass>

    <!-- Define the score function -->
    <scoreDirectorFactory>
        <constraintProviderClass>com.example.parkingsolver.solver.ParkingConstraintProvider</constraintProviderClass>
    </scoreDirectorFactory>
    <termination>
        <secondsSpentLimit>20</secondsSpentLimit>
    </termination>

</solver>